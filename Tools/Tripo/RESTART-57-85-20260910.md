# 57・85 Tripo制作の再開記録（2026-09-10）

> 更新: 補充後の2件生成は完了。結果は `RESULT-57-85-20260910.md` を参照。以下の残高0・未生成の記述は初回準備時点の履歴。

ブランチ: `codex-training`。対象: 57（イズナ）、85（ハッコ）。
UserのTripoによる制作やり直し依頼に基づき、公式画像からのベースメッシュ生成を準備した。

## 実行状況

- Python 3.12.14の専用環境 `Tools/Tripo/.venv` を作成し、requirements.txtをインストール。SDKはtripo3d 0.4.2。
- 唯一の入口 `Tools/Tripo/tripo.py` で `--list`、`doctor`、`balance` を実行。対象2フォルダが存在し、SDK・キー設定・DNS・API接続は正常。
- API残高: `balance=0, frozen=0`。生成タスクは未送信。今回の生成消費は0。
- リポジトリ運用上の概算: draft各20〜30、計40〜60クレジット。APIの確定見積額ではない。再開時は残高を再取得し、見込みが残高の30%を超える場合はAGENTS.md §12.3に従って承認を得る。
- doctorのキー先頭部分の表示を削除し、有無のみ表示するよう修正。Dropbox原本にも同じ修正の反映を提案する（原本は未変更）。

## 入力

現行CreationsDBの公式corefolder画像と、前回収録した画像のSHA-256一致を確認し、無加工で各キャラの `Training/TripoInput-20260910/` にコピーした。
出典・入力パス・SHA-256は `input-manifest-20260910.json` に記録。

2枚とも単独キャラの全体像。正面付近からのイラストで、尻尾は重なっている。画像だけで背面構造・尻尾の本数を確定できないため、生成結果を公式設定と照合する必要がある。57はウインクしており、中立表情・両目の造形は別途調整対象。

前回の制作資料では57は7尾、85は8尾。57の右肩の腕章、85の雫型ペンダント、両者のポニーテールも照合項目。最終制作前に現行DBの設定を再確認する。

## 再開コマンド（リポジトリ直下、PowerShell）

```powershell
& Tools/Tripo/.venv/Scripts/python.exe -X utf8 Tools/Tripo/tripo.py doctor
& Tools/Tripo/.venv/Scripts/python.exe -X utf8 Tools/Tripo/tripo.py balance
& Tools/Tripo/.venv/Scripts/python.exe -X utf8 Tools/Tripo/tripo.py -c 57 image2model Training/TripoInput-20260910/emstk_corefolderNTS-57-1.png --preset draft --model-seed 42 --orientation align_image
& Tools/Tripo/.venv/Scripts/python.exe -X utf8 Tools/Tripo/tripo.py -c 85 image2model Training/TripoInput-20260910/emstk_corefolderNTS-85-1.png --preset draft --model-seed 42 --orientation align_image
```

生成コマンドは残高確認後に実行する。draftは現在のconfigではTurbo-v1.0-20250506、texture=false、pbr=false。seedを記録しても世代・条件を変えた再生成で形状が完全一致するとは保証しない。
保存先は各キャラの `TripoGenerated/`。失敗時はtask_idのstatusを読み、無条件に再送しない。

## 生成後

1. previewと実メッシュの欠損・融合・貫通、耳・髪・尻尾・付属品を確認し、Userにdraftを提示。
2. 規範6体（4・16・20・22・25・93）の実VRM・制作データと同条件の正面・側面・背面で比較。今回の準備段階では、この比較は未実施。
3. draft確認後にstandard等へ進む。Blenderで共通素体、骨格、表情、材質を扱い、再利用元と変更点を記録。#4固有の肩衣装は共通化しない。
4. Blenderで整えたVRMを検証し、User監修を受ける。TripoのGLBをAssetsへ直接投入しない。

## 進捗の区別

- 点群の追加学習: 今回未実施。既存データ・重みは保持。
- 画像の作風学習: 今回未実施。生成三面図を規範モデルへ格上げしない。
- Tripoベースメッシュ: 入力準備済み、API残高0で未生成。
- 実VRMの造形・材質調整: 今回未実施。既存blend/VRMとUserの未コミット変更は保持。

Tripoへの画像条件付き生成を「規範6体による追加学習完了」とは扱わない。
